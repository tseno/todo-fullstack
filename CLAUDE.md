# CLAUDE.md

このファイルは、このリポジトリでClaude Code（AIコーディングアシスタント）が作業する際のルールをまとめたものです。
プロジェクトの目的・技術スタックはREADME.mdを参照してください。

## 進め方

当初は学習目的で「実装はユーザー自身が手を動かす」方式だったが、2026-09に方針を変更した。
**現在はAIが機能単位でまとめて実装する方式。**

- 大きな変更の前に設計を提示し、ユーザーの承認を得てから実装する
- 実装は機能単位で一気に行い、細かいステップごとの承認チェックポイントは設けない
- 実装後はテスト・ビルドで動作を検証してから報告する
- `terraform apply` / `git push` など外部に影響する操作は、実行前にユーザーへ確認する

## コーディング規約

- コードコメントは平易な日本語で記述する（何を・なぜ、が分かるように）
- コミットメッセージはConventional Commits形式（`feat:` `fix:` `docs:` `chore:` `refactor:`）を使う
- バックエンドのロジック変更は、まず失敗するテストを書いてから実装する（TDD）

## インフラ・AWSに関する方針

- なるべく低コストな構成にする（NAT Gatewayは使わない、RDSは最小インスタンスなど）
- 全リソースをTerraformで管理し、`terraform destroy`で完全に環境を破棄できる状態を維持する
- DBパスワードなどの機密情報はコードに書かず、SSMパラメータストアで管理する
